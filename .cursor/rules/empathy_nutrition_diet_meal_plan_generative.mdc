---
description: Meal plan generativo — Diet per giorno (week_plan) è l'unica fonte di pasti e % kcal; USDA solo composizione. Regola fissa, non reinterpretare.
globs: apps/web/**/nutrition/**,apps/web/lib/nutrition/**,apps/web/app/api/nutrition/**
alwaysApply: false
---

# EMPATHY Pro 2 — Meal plan generativo (regola fissa)

**Non cambiare questa semantica** senza decisione esplicita del prodotto e aggiornamento di questo file + `docs/NUTRITION_DIET_MEAL_PLAN_RULES.md` + test.

## Gerarchia (Ingest → Compute → Application)

| Piano | Cosa fa il meal plan |
|--------|----------------------|
| **Reality / profilo** | L’utente (coach) salva **Profile → Diet** per ogni giorno della settimana. |
| **Compute** | Solver energetico giorno (`daily-energy-solver`) + ripartizione slot da Diet. |
| **Application** | Composer deterministico assembla **quali alimenti** (porzioni), non **quante kcal tra pasti**. |

## Regola 1 — Diet per giorno (unica fonte ripartizione)

- Per la **data** selezionata in Nutrizione: `weekday = profileWeekDayKeyFromIsoLocal(YYYY-MM-DD)`.
- Leggere **solo** `athlete_profiles.nutrition_config.week_plan[weekday]`:
  - `meal_count_mode` (1…6, fasting, semi-*)
  - `caloric_distribution` (breakfast, lunch, dinner, snacks — % sul budget pasti)
  - `daily_macros` (CHO/PRO/FAT %)
  - `day_type_pct` (% calorie rispetto fabbisogno del giorno)
- **Implementazione canonica:** `resolveNutritionDietDay()` → `buildDietMealSlotBudgets()`.
- **Vietato:** preset globali fissi (es. 30/35/25/10) come default operativo; rollup USDA per ricalcolare % tra pasti; `caloric_split` in root che sovrascrive il week plan; salvataggio da Nutrizione che riscrive `week_plan` o `caloric_split`.

## Regola 2 — USDA / FDC (solo composizione)

- `nutrientRollup` e `nutrientsForMealPlanItem*` servono a **macro/micro/IG delle voci**, non a decidere la % kcal di colazione vs pranzo.
- Le **card pasto** mostrano **target kcal/macro del solver** (da Diet), non la somma delle voci USDA se diverge.
- Σ USDA in UI = controllo composizione, non nuova ripartizione calorica.

## Regola 3 — Numero pasti

- `meal_count_mode` nel Diet del **giorno** definisce gli slot attivi (es. 6 → include `snack_evening` con quota `snacks/3`).
- Il request al meal-plan API invia **solo gli slot** prodotti da `buildDietMealSlotBudgets` (3–6 slot), non una griglia fissa 5+placeholder.

## Regola 4 — Modulo Nutrizione non è editor Diet

- Cambiare pasti/% → **Profile → Diet** (giorno settimana corretto), poi rigenerare meal plan sulla **stessa data**.
- `handleSaveNutrition` / PATCH profilo da Nutrizione: **non** persistere `caloric_split` né `meal_plan.caloric_split` derivati dalla UI.

## Regola 5 — Fabbisogno energetico totale (non scalare con recovery/bio)

- **Fabbisogno totale del giorno** = `BMR + lifestyle + training pianificato` (kcal del consumo programmato). Quando l'eseguito è importato, sostituisce il pianificato (`Reality > Plan`).
- `nutrition_performance_integration.trainingEnergyScale` (output di `buildNutritionPerformanceIntegration`) è un **indicatore** recovery/bio (0,35–1,02): il solver lo usa SOLO per:
  - distribuzione pasti↔fueling (`mealTrainingFraction` 0,40 → 0,48 in protective);
  - composizione (CHO/h `fuelingChoScale`, bias proteine `proteinBiasPctPoints`, idratazione `hydrationFloorMultiplier`, fluido seduta `sessionFluidMultiplier`).
- **Vietato** moltiplicare il training_kcal nelle formule del fabbisogno (`totals.dailyKcal`, `totals.mealsKcal`, `totals.fuelingKcal`, `fueling.preKcal/intraKcal/postKcal`) per `trainingEnergyScale`. Le UI che mostrano `×N` su training devono dichiararlo come indicatore, non come riduzione del fabbisogno.
- Modulazione consentita sul fabbisogno: **solo `dietDayMealsScalePct`** (Profile Diet → `day_type_pct`).

## File canonici (non duplicare pipeline)

- `apps/web/lib/nutrition/resolve-nutrition-diet-day.ts`
- `apps/web/lib/nutrition/diet-meal-slot-budgets.ts`
- `apps/web/lib/nutrition/daily-energy-solver.ts` (`dietDayMealsScalePct`)
- `apps/web/lib/nutrition/deterministic-meal-plan-from-request.ts`
- `apps/web/app/api/nutrition/intelligent-meal-plan/route.ts`

## Regola 6 — Memoria alimenti (canonical + USDA) è sorgente unica

- Il composer **pesca** gli alimenti da `CANONICAL_FOOD_TABLE` + cache USDA FDC (`nutrition_fdc_foods`) via `inferCanonicalFoodKeyPreferName` → `nutrientsForMealPlanItem*`. **Nessun item può essere emesso con un name che non risolve a un canonical key valido**: `compositionStatus="unresolved"` = bug strutturale (0 kcal silenzioso).
- Quando aggiungi un nuovo alimento ai pool del composer (`BREAKFAST_BEVERAGES`, `BREAKFAST_FRUITS`, archetipi cena/pranzo/snack, varianti veg/vegan):
  1. Aggiungi una **row USDA-like** in `CANONICAL_FOOD_TABLE` (`apps/web/lib/nutrition/canonical-food-composition.ts`).
  2. Aggiungi una **rule INFER** in `INFER_RULES` posizionata PRIMA delle rule generiche che potrebbero catturarla.
  3. Se è un **liquido** (latte, bevanda vegetale, yogurt da bere): aggiungi il key a `LIQUIDS_AS_GRAMS_KEYS` (server + client) per il scaling `ml → g`.
  4. (Opzionale) Mappa USDA FDC ID in `canonical-food-fdc-aliases.ts` se vuoi che la cache server-side scali sui valori USDA invece del TS table.

## Regola 7 — Composizione CHO per slot (regola nutrizionista, vincolante)

**Colazione**:

- CHO target del pasto **< 130 g** → 1 fonte CHO complessa principale (cereali, avena, muesli, pane tostato, fette biscottate) + frutta.
- CHO target **≥ 130 g** → **2 fonti CHO solide complesse distinte** (es. cereali soffiati/muesli/avena/porridge **+ pane tostato/fette biscottate**). La quota della 1ª fonte si riduce per fare spazio alla 2ª.

**Pranzo / Cena**:

- CHO target del pasto **≤ 100 g** → 1 fonte CHO principale; pane può comparire come carb principale (cena leggera) o non comparire affatto.
- CHO target **> 100 g** → **pane NON può essere il carb principale**. La fonte principale deve essere un amido complesso: **pasta, riso, farro, orzo, quinoa, patate**.
- CHO target **≥ 130 g** → **2 fonti CHO**: principale complessa (sopra) **+ secondaria** (pane integrale **o** focaccia). La 2ª fonte (`paneFinalG`) scala con il target: 20–35 g (100–130), 40–80 g (130–180), 70–110 g (>180).

**Implementazione**:

- `apps/web/lib/nutrition/mediterranean-meal-composer.ts` → filtro `carbOrder` esclude `"pane"` quando `slot ∈ {lunch, dinner}` e `m.carbsG > 100`; `paneG` scala su `m.carbsG`.
- `apps/web/lib/nutrition/breakfast-meal-archetypes.ts` → `appendBreakfastSecondaryCarb()` aggiunge la 2ª fonte CHO solida quando `m.carbsG ≥ 130`; ogni archetipo riduce la quota della 1ª fonte (ratio CHO `0.36–0.43` invece di `0.55–0.70`) per equilibrare le kcal.
- Pool carb principale lunch/dinner: `pasta | riso | patate | farro | quinoa | pane` (5 complessi + pane solo cene leggere). Garantisce ≥ 14 selezioni/settimana sotto `MAX_STAPLE_USES_PER_WEEK=3`.

## Test minimi obbligatori su modifica

- `resolve-nutrition-diet-day.test.ts`
- `diet-meal-slot-budgets.test.ts`
- `meal-plan-memory-guardrail.test.ts` — esercita TUTTI gli slot × dietType × kcal range e fallisce se un item del composer non risolve nella memoria canonica. **Copre anche Regola 7**: lunch/dinner CHO > 100g non usa pane come principale; lunch/dinner CHO ≥ 130g ha ≥ 2 item `cho_heavy`; breakfast CHO ≥ 130g ha ≥ 2 fonti CHO solide complesse.
- `canonical-food-composition.test.ts` — regression test su rule INFER specifiche e scaling per nome alimento.
