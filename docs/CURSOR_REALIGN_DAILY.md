# Cursor — riallineamento quotidiano

**Uso:** incolla il blocco sotto in chat all’inizio della giornata o quando il task è piccolo.  
**Oppure:** copialo in **Cursor → Settings → Rules → User rules** (resta sempre attivo).

---

```
EMPATHY — check rapido prima di lavorare:
- Workspace giusto? V1 = nextjs-empathy-pro | Pro 2.0 = empathy-pro-2-cursor (non mescolare).
- Generativo: 4 piani Ingest → Compute (motori+twin) → Interpretation (knowledge/trace; AI solo interpretazione/evidenza strutturata) → Application. AI non sostituisce motori né genera sessioni/piani/numeri al posto del pipeline; un solo builder canonico per singola sessione. Reality > Plan, Physiology > UI, carico interno > esterno.
- Schema/SQL/API: mai fix isolato senza migration + uso nel codice + impatto V1/Pro 2 se condividono DB.
- UI Pro 2: canone pagina in questo repo `docs/PRO2_UI_PAGE_CANON.md` (Builder rich).
- Calendario / giornata Pro 2: stessa API `planned-window`; contratto builder in notes (`BUILDER_SESSION_JSON::`); UI `TrainingCalendarPageView` + `TrainingSessionPageView` + `CalendarPlannedBuilderDetail` (non parallel data path).
```
