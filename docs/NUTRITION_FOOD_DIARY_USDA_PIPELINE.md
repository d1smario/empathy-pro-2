# Diario alimentare + USDA — puntatore

La **specifica completa** (fasi foto temporanea, finalize, delete, contratto voci diario, rollout non-breaking) è mantenuta nel repository **Empathy V1** per evitare duplicati:

**`nextjs-empathy-pro` → `docs/NUTRITION_FOOD_DIARY_USDA_PIPELINE.md`**

Su GitHub: stesso file nel repo `d1smario/nextjs-empathy-pro` (path sopra).

L’implementazione Pro 2.0 userà `packages/domain-nutrition` e `packages/contracts` allineati a quel documento.
