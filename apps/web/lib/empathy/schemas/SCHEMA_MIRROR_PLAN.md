# Schema mirror plan (Pro 2)

Gradual re-export from `@empathy/contracts` into `apps/web/lib/empathy/schemas/`.

**Done (2026-05):** `memory.ts` → re-export tipi da `@empathy/contracts` (cutover memory graph).

Next domains when touching related API routes: nutrition constraints, physiology state, twin.

Current source of truth: `packages/contracts/src/schemas/`.
